---
title: Daily writing on security, automation, and taste
---

This site is the publishing source of truth for Gagan Yalamuri.

New posts can be written directly in Markdown under `_posts/`, or generated by Codex on a schedule and committed automatically.

## Recent posts

{% assign posts = site.posts %}
{% if posts.size > 0 %}
<ul class="post-list">
{% for post in posts %}
  <li>
    <a href="{{ post.url }}">{{ post.title }}</a><br>
    <span>{{ post.date | date: "%B %-d, %Y" }}{% if post.summary %} · {{ post.summary }}{% endif %}</span>
  </li>
{% endfor %}
</ul>
{% else %}
No posts yet.
{% endif %}
